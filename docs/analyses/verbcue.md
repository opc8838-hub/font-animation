# Effect analysis: `verbcue`

Source: `动效2.qt` 6.90s 720×1556. Crop social chrome.

Phases: trash object → `Move it.` letters scatter in → `Upscale it.` pixel wipe → `Share it.` split close → price hold.

New page, not merged with 动效6. Shared only `fx-kit.js` playback/export.
